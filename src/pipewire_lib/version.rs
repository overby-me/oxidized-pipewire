// The pipewire C API version string we claim to implement.
// Tools print this for `--version`; tests normalize the version away so
// it doesn't have to match the upstream package version exactly.
pub const PIPEWIRE_API_VERSION: &str = "1.6.3";
