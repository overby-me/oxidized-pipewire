[package]
name = "rust-pipewire"
version = "0.1.0"
edition = "2024"
description = "PipeWire-compatible multimedia graph daemon and tools written in Rust"
license = "MIT"

[[bin]]
name = "rust-pipewire"
path = "src/main.rs"

[dependencies]

[profile.release]
strip = true
lto = true
